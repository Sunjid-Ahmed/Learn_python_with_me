#waf to convert USD to INR
dollar=int(input("enter the ammount of dollar:"))
 
def conv_usd (usd_val):
    inr_val=usd_val*83
    print(usd_val, "USD =",inr_val, "INR") 
conv_usd(dollar)    

#usd to bdt

def conv_usd (usd_val):
    bdt_val=usd_val*118
    print(usd_val, "USD =",bdt_val, "BDT") 
conv_usd(dollar)     